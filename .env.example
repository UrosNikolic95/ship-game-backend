# PostgreSQL connection used to persist world and player game data.
# Copy this file to `.env` and fill in the values for your database.
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=secret
DB_NAME=shipgame

# When true, TypeORM creates/updates tables automatically on startup.
DB_SYNCHRONIZE=true
